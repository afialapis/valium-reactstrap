-- ver errores en el select,
  y porque coño el click en el checkbox va al select

-- en rdp estamos añadiendo hiddenInputRef, parece
   que es lo que nos interesa linkar a valium, no?  ==> no parece funcionar

  -- en rdp estamos viendo tb elonClikOutside(), igual esta lanzando validaciones
    innecesarias, cuando los licks no tienen nada que ver con el RDP




-- InputFloating

  ? commonize how it handles [un]controlled values
  ? onChange sends a 2nd parameter confirmed... commonize it?

  - add FloatingSum field here
  

- Feedback visibility:
  Broken for VInputSelectSearch

- Inputs need onChange() in order to the is-unsave class to work.
  Makes sense?